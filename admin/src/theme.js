// theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  direction: "rtl", // Set this to 'ltr' or 'rtl' based on your language settings
  typography: {
    fontFamily: "Roboto, sans-serif",
  },
  
});

export default theme;
